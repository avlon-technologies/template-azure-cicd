name: Deploy

on:
  workflow_call:
    inputs:
      environment:
        description: GitHub environment name (dev | stg | prod)
        type: string
        required: true
      webapp-name:
        description: Azure App Service name to deploy to
        type: string
        required: true
      azure-client-id:
        description: App Registration client ID for OIDC login
        type: string
        required: true
      build-label:
        description: Build label (YYYYMMDD.run_number) used to tag the deployment commit
        type: string
        required: true

jobs:
  deploy:
    name: Deploy to ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write
      contents: write

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: webapp-publish
          path: ./publish

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ inputs.azure-client-id }}
          tenant-id: fb0af7d1-8a83-48bc-8d2d-cd92034c79d4
          subscription-id: c0985859-a9bb-481e-b71d-63c3a72ed7cb

      - name: Deploy to App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ inputs.webapp-name }}
          package: ./publish

      - name: Tag deployment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: `refs/tags/build/${{ inputs.environment }}/${{ inputs.build-label }}`,
              sha: context.sha
            })
